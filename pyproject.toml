[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "vmware-storage"
version = "1.5.24"
description = "VMware vSphere storage management: datastores, iSCSI, vSAN. Domain-focused MCP skill."
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [{ name = "Wei Zhou", email = "wei-wz.zhou@broadcom.com" }]
dependencies = [
    "pyvmomi>=8.0.3.0,<10.0",
    "pyaml>=24.0,<27.0",
    "typer>=0.12,<1.0",
    "rich>=13.0,<15.0",
    "python-dotenv>=1.0,<2.0",
    "mcp[cli]>=1.0,<2.0",
    "vmware-policy>=1.0.0,<2.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0,<10.0",
    "pytest-cov>=5.0,<8.0",
    "ruff>=0.5,<1.0",
]

[project.scripts]
vmware-storage = "vmware_storage.cli:app"
vmware-storage-mcp = "mcp_server.server:main"

[tool.hatch.build.targets.wheel]
packages = ["vmware_storage", "mcp_server"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "unit: Unit tests",
    "integration: Integration tests (require vCenter)",
]
